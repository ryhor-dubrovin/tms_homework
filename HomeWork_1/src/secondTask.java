public class secondTask {
    public static void main(String[] args) {
        double firstPart;
        double secondPart;
        double result;

        firstPart = (1D/1D/17D)/(Math.pow((Math.pow(3D,3D)+Math.pow(4D,3D)),4D));
        secondPart = 1000D/1D/17D;

        result = firstPart * secondPart;

        System.out.println(firstPart + " * " + secondPart + " = " + result);
    }
}
